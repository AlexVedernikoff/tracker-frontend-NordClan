import React from 'react';
import * as css from './Repeat.scss';

const Repeat = () => (
  <div>
    <section>
      <h1 className={css.testClass}>Repeat</h1>
    </section>
  </div>
);

export default Repeat;
