import React from 'react';
import * as css from './Portfolio.scss';

const Portfolio = () => (
  <div>
    <section>
      <h1 className={css.testClass}>Portfolio</h1>
    </section>
  </div>
);

export default Portfolio;
