import React from 'react';
import * as css from './Dashboard.scss';

const Dashboard = () => (
  <div>
    <section>
      <h1 className={css.testClass}>Dashboard</h1>
    </section>
  </div>
);

export default Dashboard;
