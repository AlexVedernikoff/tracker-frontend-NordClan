import React from 'react';
import * as css from './Tasks.scss';

const Tasks = () => (
  <div>
    <section>
      <h1 className={css.testClass}>Tasks</h1>
    </section>
  </div>
);

export default Tasks;
