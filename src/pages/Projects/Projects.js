import React from 'react';
import * as css from './Projects.scss';

const Projects = () => (
  <div>
    <section>
      <h1 className={css.testClass}>Projects</h1>
    </section>
  </div>
);

export default Projects;
