const NOT_SET = 1;
const BLOCKER = 2;
const CRITICAL = 3;
const MAJOR = 4;
const NORMAL = 5;
const MINOR = 6;
const TRIVIAL = 7;

export const TEST_CASE_SEVERITIES = Object.freeze({
  NOT_SET,
  BLOCKER,
  CRITICAL,
  MAJOR,
  NORMAL,
  MINOR,
  TRIVIAL
});
