// This event listener is used in order to prevent page reload on form submit.
// It helps reducing extra code in form submit handlers.
document.addEventListener('submit', event => {
  event.preventDefault();
});
